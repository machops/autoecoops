# Contracts-L1 責任邊界與依賴關係圖

## 📊 垂直分層架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                        🌐 外部接口層                             │
│  責任: 接收外部請求、返回響應、格式轉換                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  apps/web/                    apps/api/                        │
│  ┌─────────────────┐         ┌─────────────────┐              │
│  │  Next.js Pages  │────HTTP──▶│  Express Routes│              │
│  │  React Components│         │  Controllers   │              │
│  └─────────────────┘         └─────────────────┘              │
│         │                             │                        │
│         │ 使用                        │ 調用                    │
│         ▼                             ▼                        │
├─────────────────────────────────────────────────────────────────┤
│                       💼 業務邏輯層                              │
│  責任: 業務規則實施、流程編排、數據驗證                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│         apps/api/src/services/                                 │
│  ┌──────────┬──────────┬──────────┬──────────┐                │
│  │AuthService│ContractSvc│AnalysisSvc│SearchSvc│                │
│  └────┬─────┴─────┬────┴─────┬────┴────┬─────┘                │
│       │           │          │         │                       │
│       │ 依賴      │ 依賴     │ 依賴    │ 依賴                   │
│       ▼           ▼          ▼         ▼                       │
├─────────────────────────────────────────────────────────────────┤
│                      🔧 共享引擎層                               │
│  責任: 可重用業務能力、跨應用共享邏輯                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  packages/ai-engine/       packages/semantic-engine/           │
│  ┌────────────────┐       ┌────────────────┐                  │
│  │ AI Router      │       │ Vector Search  │                  │
│  │ Cost Tracker   │       │ Graph Query    │                  │
│  │ Model Manager  │       │ Hybrid Fusion  │                  │
│  └────────────────┘       └────────────────┘                  │
│         │                         │                            │
│         │ 依賴                    │ 依賴                        │
│         ▼                         ▼                            │
├─────────────────────────────────────────────────────────────────┤
│                     📚 共享工具層                                │
│  責任: 型別定義、工具函數、常量、驗證器                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  packages/shared/              packages/ui/                    │
│  ┌────────────────┐           ┌────────────────┐              │
│  │ Types          │           │ UI Components  │              │
│  │ Utils          │           │ Hooks          │              │
│  │ Constants      │           │ Styles         │              │
│  │ Validators     │           └────────────────┘              │
│  └────────────────┘                                            │
│         │                                                      │
│         │ 依賴                                                  │
│         ▼                                                      │
├─────────────────────────────────────────────────────────────────┤
│                    💾 數據持久層                                 │
│  責任: 數據模型定義、數據庫操作、遷移管理                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  packages/database/                                            │
│  ┌────────────────────────────────┐                            │
│  │ Prisma Schema                  │                            │
│  │ Migrations                     │                            │
│  │ Seed Data                      │                            │
│  └────────────────────────────────┘                            │
│         │                                                      │
│         │ 連接                                                  │
│         ▼                                                      │
├─────────────────────────────────────────────────────────────────┤
│                   🗄️  外部服務層                                │
│  責任: 第三方服務、資料庫、快取、儲存                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  PostgreSQL    Redis    Neo4j    Pinecone    OpenAI    S3     │
│  └────┬───┘  └──┬──┘  └──┬──┘  └───┬───┘  └──┬──┘  └─┬─┘     │
│       └──────────┴─────────┴──────────┴──────────┴─────┘      │
│                    外部基礎設施                                  │
└─────────────────────────────────────────────────────────────────┘

📖 依賴規則:
  ✅ 上層可以依賴下層
  ❌ 下層不可依賴上層
  ❌ 同層之間不可直接依賴
```

## 🔀 水平模組邊界圖

```
┌────────────── 應用層 (Apps) ──────────────┐
│                                            │
│  ┌─────────┐    ┌─────────┐    ┌────────┐│
│  │apps/web │    │apps/api │    │apps/   ││
│  │         │    │         │    │worker  ││
│  │Next.js  │    │Express  │    │Bull    ││
│  │React    │    │Node.js  │    │Queue   ││
│  └────┬────┘    └────┬────┘    └───┬────┘│
│       │              │              │     │
│       │HTTP API      │Message Queue │     │
│       │              │              │     │
│       └──────────────┼──────────────┘     │
│                      │                    │
└──────────────────────┼────────────────────┘
                       │
                       │ 都依賴
                       ▼
┌────────────── 共享層 (Packages) ───────────┐
│                                            │
│  ┌─────────┐  ┌─────────┐  ┌───────────┐ │
│  │packages/│  │packages/│  │packages/  │ │
│  │shared   │  │ui       │  │database   │ │
│  └─────────┘  └─────────┘  └───────────┘ │
│                                            │
│  ┌──────────────┐  ┌──────────────┐      │
│  │packages/     │  │packages/     │      │
│  │ai-engine     │  │semantic-engine│     │
│  └──────────────┘  └──────────────┘      │
│                                            │
│  ⚠️  禁止跨套件直接依賴                    │
│  ✅  透過明確介面通訊                      │
└────────────────────────────────────────────┘

📖 隔離規則:
  ❌ apps/web 不可直接導入 apps/api 模組
  ❌ packages/ai-engine 不可直接導入 packages/semantic-engine
  ✅ 透過 packages/shared 中定義的介面通訊
```

## 🎯 功能模組責任矩陣

```
┌─────────────┬───────────┬───────────┬───────────┬───────────┐
│   功能模組   │  前端Web  │  後端API  │  Worker   │  Packages │
├─────────────┼───────────┼───────────┼───────────┼───────────┤
│ 使用者介面   │    ✅ 主責 │     ❌    │     ❌    │  🔧 組件  │
│ 路由導航     │    ✅ 主責 │     ❌    │     ❌    │     ❌    │
│ 狀態管理     │    ✅ 主責 │     ❌    │     ❌    │     ❌    │
│ API 呼叫     │    🔧 調用│    ✅ 主責│     ❌    │     ❌    │
├─────────────┼───────────┼───────────┼───────────┼───────────┤
│ 業務邏輯     │     ❌    │    ✅ 主責│     ❌    │  🔧 引擎  │
│ 數據驗證     │    🔧 前端│    ✅ 後端│     ❌    │  🔧 規則  │
│ 認證授權     │    🔧 UI  │    ✅ 主責│     ❌    │  🔧 JWT   │
│ 錯誤處理     │    🔧 UI  │    ✅ 主責│    🔧 任務│     ❌    │
├─────────────┼───────────┼───────────┼───────────┼───────────┤
│ 檔案上傳     │    🔧 UI  │    ✅ 主責│     ❌    │     ❌    │
│ AI 分析      │     ❌    │    🔧 觸發│    ✅ 主責│  🔧 引擎  │
│ 文件解析     │     ❌    │     ❌    │    ✅ 主責│  🔧 解析器│
│ 語義索引     │     ❌    │     ❌    │    ✅ 主責│  🔧 引擎  │
│ 通知發送     │    🔧 UI  │    🔧 觸發│    ✅ 主責│     ❌    │
├─────────────┼───────────┼───────────┼───────────┼───────────┤
│ 型別定義     │    🔧 使用│    🔧 使用│    🔧 使用│    ✅ 主責│
│ 工具函數     │    🔧 使用│    🔧 使用│    🔧 使用│    ✅ 主責│
│ UI 組件      │    🔧 使用│     ❌    │     ❌    │    ✅ 主責│
│ AI 引擎      │     ❌    │    🔧 使用│    🔧 使用│    ✅ 主責│
│ 語義引擎     │     ❌    │    🔧 使用│    🔧 使用│    ✅ 主責│
│ 資料模型     │     ❌    │    🔧 使用│    🔧 使用│    ✅ 主責│
└─────────────┴───────────┴───────────┴───────────┴───────────┘

圖例:
  ✅ 主責 - 此模組負責實作該功能
  🔧 使用/輔助 - 此模組使用該功能或提供輔助
  ❌ 不涉及 - 此模組不應涉及該功能
```

## 🔐 數據流與責任邊界

```
┌──────────────────── 契約上傳與分析流程 ────────────────────┐
│                                                            │
│  [使用者]                                                  │
│     │                                                      │
│     │ 1. 選擇檔案                                          │
│     ▼                                                      │
│  ┌──────────────┐                                         │
│  │ apps/web     │                                         │
│  │ UploadZone   │  責任: UI渲染、拖放處理、進度顯示         │
│  └──────┬───────┘                                         │
│         │ 2. POST /api/contracts/upload                   │
│         ▼                                                  │
│  ┌──────────────┐                                         │
│  │ apps/api     │                                         │
│  │ POST Handler │  責任: 請求驗證、配額檢查                │
│  └──────┬───────┘                                         │
│         │ 3. 調用服務                                      │
│         ▼                                                  │
│  ┌──────────────┐                                         │
│  │ UploadService│  責任: S3上傳、資料庫記錄                │
│  └──────┬───────┘                                         │
│         │ 4. 發布事件                                      │
│         ▼                                                  │
│  ┌──────────────┐                                         │
│  │ Message Queue│  責任: 異步任務分發                      │
│  └──────┬───────┘                                         │
│         │ 5. 消費任務                                      │
│         ▼                                                  │
│  ┌──────────────┐                                         │
│  │ apps/worker  │                                         │
│  │ AnalysisJob  │  責任: 任務執行編排                      │
│  └──────┬───────┘                                         │
│         │ 6. 調用引擎                                      │
│         ▼                                                  │
│  ┌──────────────────────────────┐                         │
│  │ packages/ai-engine           │                         │
│  │ AIRouter.analyze()           │  責任: AI調用、成本追蹤  │
│  └──────┬───────────────────────┘                         │
│         │ 7. 儲存結果                                      │
│         ▼                                                  │
│  ┌──────────────┐                                         │
│  │ packages/    │                                         │
│  │ database     │  責任: 數據持久化                        │
│  └──────┬───────┘                                         │
│         │ 8. 發送通知                                      │
│         ▼                                                  │
│  ┌──────────────┐                                         │
│  │ apps/worker  │                                         │
│  │ NotifyJob    │  責任: 郵件/推送通知                     │
│  └──────────────┘                                         │
│                                                            │
│  📖 邊界規則:                                              │
│    • 前端不可直接調用 AI 引擎                              │
│    • 後端不可直接操作前端狀態                              │
│    • Worker 不可直接響應 HTTP 請求                        │
│    • Packages 不可依賴 Apps                               │
└────────────────────────────────────────────────────────────┘
```

## 🔒 敏感數據處理責任

```
┌─────────────── 數據安全責任分層 ───────────────┐
│                                                │
│  前端層 (apps/web)                             │
│  ┌──────────────────────────────────────┐     │
│  │ ❌ 不儲存敏感數據                      │     │
│  │ ❌ 不處理密碼雜湊                      │     │
│  │ ✅ 只儲存 JWT 令牌 (短期)             │     │
│  │ ✅ 使用 HTTPS 傳輸                    │     │
│  └──────────────────────────────────────┘     │
│         │ 加密傳輸                              │
│         ▼                                      │
│  後端層 (apps/api)                             │
│  ┌──────────────────────────────────────┐     │
│  │ ✅ 驗證所有輸入                        │     │
│  │ ✅ 使用 bcrypt 雜湊密碼                │     │
│  │ ✅ 生成 JWT (含過期時間)              │     │
│  │ ❌ 不記錄敏感資訊                      │     │
│  │ ✅ 實施速率限制                        │     │
│  └──────────────────────────────────────┘     │
│         │ 加密儲存                              │
│         ▼                                      │
│  資料庫層 (packages/database)                  │
│  ┌──────────────────────────────────────┐     │
│  │ ✅ 加密敏感欄位 (password, apiKey)     │     │
│  │ ✅ 備份加密                            │     │
│  │ ✅ 存取日誌記錄                        │     │
│  │ ❌ 絕不明文儲存密碼                    │     │
│  └──────────────────────────────────────┘     │
│                                                │
└────────────────────────────────────────────────┘

📖 責任分配:
  • 前端: 基本驗證、安全傳輸
  • 後端: 深度驗證、加密處理、存取控制
  • 資料庫: 加密儲存、審計日誌
```

## 📋 開發檢查清單模板

### 新增功能檢查清單

```markdown
## 功能: [功能名稱]

### 1. 責任邊界確認
- [ ] 功能屬於哪一層? (接口/業務/引擎/工具/數據)
- [ ] 主要責任模組? (web/api/worker/packages)
- [ ] 是否越界? (依賴檢查)
- [ ] 是否重複? (已有類似功能?)

### 2. 文件結構確認
- [ ] 目錄位置正確? 
- [ ] 文件命名符合規範?
- [ ] 是否需要測試文件?
- [ ] 是否需要更新文檔?

### 3. 依賴關係確認
- [ ] 依賴方向正確? (上→下)
- [ ] 沒有循環依賴?
- [ ] 使用共享型別?
- [ ] 介面定義清晰?

### 4. 命名規範確認
- [ ] 變數/函數命名 (camelCase)
- [ ] 類型/介面命名 (PascalCase)
- [ ] 常量命名 (UPPER_SNAKE_CASE)
- [ ] API 鍵名 (snake_case)

### 5. 安全性確認
- [ ] 輸入驗證完整?
- [ ] 敏感數據加密?
- [ ] 權限檢查到位?
- [ ] 錯誤處理安全?

### 6. 測試確認
- [ ] 單元測試覆蓋?
- [ ] 整合測試?
- [ ] 邊界情況測試?
- [ ] 性能測試?
```

## 🎓 責任邊界反模式 (Anti-Patterns)

### ❌ 反模式 1: 層級越界

```typescript
// ❌ 錯誤: 前端直接操作資料庫
// apps/web/src/components/ContractList.tsx
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();
const contracts = await prisma.contract.findMany();

// ✅ 正確: 前端透過 API
// apps/web/src/components/ContractList.tsx
const response = await fetch('/api/contracts');
const contracts = await response.json();
```

### ❌ 反模式 2: 職責不清

```typescript
// ❌ 錯誤: 一個服務做太多事
class UserService {
  login() { }              // 認證
  sendEmail() { }          // 郵件
  uploadAvatar() { }       // 檔案
  analyzeContract() { }    // AI分析
  generateReport() { }     // 報表
}

// ✅ 正確: 單一職責拆分
class AuthService {
  login() { }
  verifyToken() { }
}

class EmailService {
  sendWelcome() { }
  sendReset() { }
}

class FileService {
  upload() { }
  delete() { }
}
```

### ❌ 反模式 3: 緊耦合

```typescript
// ❌ 錯誤: 直接依賴具體實作
class AnalysisService {
  async analyze(text: string) {
    const openai = new OpenAI(process.env.OPENAI_KEY);
    return await openai.complete(text);
  }
}

// ✅ 正確: 依賴抽象介面
interface AIProvider {
  complete(text: string): Promise<string>;
}

class AnalysisService {
  constructor(private aiProvider: AIProvider) {}
  
  async analyze(text: string) {
    return await this.aiProvider.complete(text);
  }
}
```

### ❌ 反模式 4: 共享可變狀態

```typescript
// ❌ 錯誤: 全域可變狀態
// packages/shared/src/state.ts
export let currentUser: User | null = null;

// ✅ 正確: 不可變數據 + 狀態管理
// apps/web/src/store/auth.ts
import { create } from 'zustand';

const useAuthStore = create<AuthState>((set) => ({
  user: null,
  setUser: (user) => set({ user })
}));
```

---

這份完整的目錄樹規劃確保了專案的:
- ✅ **清晰的責任邊界** - 每個模組職責單一明確
- ✅ **嚴格的依賴方向** - 上層依賴下層,杜絕循環
- ✅ **統一的命名規範** - 減少認知負擔
- ✅ **完整的元數據** - 自說明的程式碼
- ✅ **可擴展的架構** - 支援漸進式演進
